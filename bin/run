#!/usr/bin/env ruby 
require 'sqlite3'

require_relative "../lib/song.rb"
require_relative "../config/environment.rb"


song = Song.new(name: "Hello", album: "25")
song.save
song2 = Song.new(name: "What's up", album: "Bro") 
puts song2
puts DB[:conn].execute("SELECT * FROM songs")
